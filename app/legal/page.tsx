import { redirect } from 'next/navigation';

// Legal page archived — being refined before going live.
// Full content preserved in app/_archived_legal/
export default function LegalPage() {
  redirect('/');
}
